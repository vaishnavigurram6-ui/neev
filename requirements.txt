# Neev — the agent pipeline's dependencies now live in src/agents/pyproject.toml.
# This file is kept so `pip install -r requirements.txt` still works from the
# repo root; it performs an editable install of the agents package.
#
# The backend has its own, disjoint dependency set in src/backend/pyproject.toml —
# it deliberately does NOT carry google-adk.
-e ./src/agents
