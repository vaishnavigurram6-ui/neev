# Secrets — never commit. Covers .env, .env.local, .env.disabled-backup, etc.
.env*
!.env.example

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/

# Node / Next.js
node_modules/
.next/
out/

# ADK local session state
.adk/

# Local demo photos (add manually per SETUP)
demo_assets/

# macOS
.DS_Store

# Local scratch, not part of the project
_to_delete/

# Batch agent worktrees — branches live on the remote, these are scratch
.claude/worktrees/

# Raw captured pipeline runs. Saved before parsing so a billed run survives a
# validation failure (scripts/record_golden_run.py --from-raw). May hold model
# output verbatim, so it stays local.
.golden_runs/

# Editor swap files.
*.sw[a-p]

# Synthetic BoQ PDFs. Regenerate with `scripts/synthetic_boqs.py generate --pdf`
# (needs reportlab). Not committed: reportlab embeds a CreationDate, so the
# bytes churn on every run. manifest.json IS deterministic and is the source of
# truth -- scoring reads it and needs no PDFs at all.
fixtures/synthetic/*.pdf
