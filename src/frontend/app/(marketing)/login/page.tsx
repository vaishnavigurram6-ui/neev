// Placeholder. The Login screen is built in Task 14: role toggle (home builder /
// lender), phone + OTP, and the "link your bank sent" fallback. No passwords.
export const metadata = { title: 'Log in' };

export default function LoginPage() {
  return (
    <main className="mx-auto max-w-[1280px] px-7 pb-20 pt-9">
      <p className="text-[13px] text-sub">
        Log in — phone and OTP sign-in arrives in Task 14.
      </p>
    </main>
  );
}
